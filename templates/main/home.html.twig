{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}

{% block body %}
    <section class="home">
        <ul>
            {% for ilot in ilots %}
                <li>
                    <a href="{{ path('ilot_of', { 'nomURL' : ilot.nomURL }) }}" title="Nom d'ilot">{{ ilot.nomIRL }}</a>
                </li>
            {% endfor %}
        </ul>
    </section>
{% endblock %}
