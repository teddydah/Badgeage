<div id="print-info" class="pop-up none">
    <div>
        <i id="close" class="fa-solid fa-xmark" title="Fermer la fenêtre"></i>
        <i id="info" class="fa-solid fa-circle-info"></i>
        {% if ilot.nomURL is same as('LaqEtiqOF') %}
            <p>Scannez un OF pour imprimer une étiquette avec numéro d'OF + nom client</p>
        {% elseif ilot.nomURL is same as('LaqEtiqRAL') %}
            <p>Badger code-barre du RAL pour imprimer des étiquettes par lot de 20</p>
        {% endif %}
    </div>
</div>
<div id="printer-item" class="printer-item">
    <div class="form">
        {{ form_start(form, { 'attr' : { 'novalidate' : 'novalidate' }}) }}
        {{ form_label(form.numero) }}
        <div class="form-input mb-3">
            {{ form_widget(form.numero) }}
            <i id="circle-info" class="fa-solid fa-circle-info" title="Information impression"></i>
        </div>
        {{ form_end(form) }}
    </div>
    {% include('_flash_messages.html.twig') %}
    <div id="printer-item" class="printer-item">
        <div class="printer-info">
            <i class="fa-solid fa-print"></i>
            <div>
                <span><strong>{{ ilot.printer.nom }}</strong> @{{ ilot.printer.ip }}</span>
                {# TODO : définir le statut #}
                <span>Statut : <strong class="isActive">Active</strong></span>
            </div>
        </div>
    </div>
    <div class="boutons">
        <div class="item-1">
            <a class="refresh bouton" href="{{ path('badgeage_view', { 'nomURL' : ilot.nomURL }) }}"
               title="Page options"><span>Rafraîchir</span></a>
        </div>
        <div class="item-3">
            {% if ilot.nomURL is same as('LaqEtiqOF') %}
                <a class="ilot item-btn-6 bouton"
                   href="{{ path('badgeage_view', { 'nomURL' : 'LaqEtiqRAL' }) }}"
                   title="Ilot Etiquettes RAL">Etiquettes RAL</a>
            {% elseif ilot.nomURL is same as('LaqEtiqRAL') %}
                <a class="ilot item-btn-6 bouton"
                   href="{{ path('badgeage_view', { 'nomURL' : 'LaqEtiqOF' }) }}"
                   title="Ilot Etiquettes OF">Etiquettes OF</a>
            {% endif %}
        </div>
        <div class="item-2">
            <a class="prev item-btn-1 bouton" id="btn_retour" href="{{ path }}"
               role="button"
               title="Page précédente"><span>Précédent</span></a>
            <a class="home item-btn-2 bouton" href="{{ path('main_home') }}"
               title="Page d'accueil"><span>Accueil</span></a>
        </div>
    </div>
</div>