{% extends 'base.html.twig' %}

{% block title %}Hello PaintController!{% endblock %}

{% block body %}
    <section class="ilot">
        <div class="form">
            {{ form_start(form) }}
            <label>Badgeage OF {{ ilot.nomIRL }}</label>
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
        {% include('_flash_messages.html.twig') %}
        <div class="boutons">
            <div class="item-1">
                {# TODO #}
                <a class="prev bouton" id="btn_retour_idx" onclick="history.back()" target="_self" role="button"
                   title="Page précédente">Précédent</a>
                <a class="home bouton" href="{{ path('main_home') }}" title="Page d'accueil">Accueil</a>
            </div>
            <div class="item-2">
                <a class="print bouton" href="{{ path('impression_etiquette', { 'nomURL' : ilot.nomURL }) }}"
                   title="Page impression">Impression</a>
                <a class="photo bouton" href="#" title="Page photo">Photo</a>
            </div>
            <div class="item-3">
                <a class="options bouton" href="{{ path('ilot_options', { 'nomURL' : ilot.nomURL }) }}"
                   title="Page options">Options</a>
            </div>
        </div>
    </section>
{% endblock %}
